# --- Secrets / config (entire folder, except keeper file) ---
secrets/*
!secrets/.gitkeep
!secrets/README.md
.env
.streamlit/secrets.toml
auth_config.yaml

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Streamlit cache ---
.streamlit/cache/

# --- IDE / editors ---
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# --- Agent / tooling state (local scratch, not shared) ---
.omc/
.claude/settings.local.json
CLAUDE.local.md

# --- OS ---
Thumbs.db

# --- Logs / runtime ---
*.log
logs/

# --- Generated PDF/output (สร้างชั่วคราว ไม่เก็บใน repo) ---
generated/
*.pdf
output/

# Local image storage (รูปสินค้า/สต็อก — เก็บบนเครื่องผ่าน lib/storage.py ไม่ commit)
data/

# Self-signed TLS certs for LAN HTTPS (camera/getUserMedia needs secure context)
certs/
*.pem

# --- Reference artefacts (real business data / large binaries — keep local, don't commit) ---
# ต้นฉบับบิล/ข้อมูลขายจริง ใช้เทียบ layout เท่านั้น (ดู memory bill-print-template-source)
ส่งโรงเรียน2022.xlsx
bill_example.jpg
